{
  "name": "smartcart-monorepo",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "build": "npm run build -w frontend",
    "dev": "npm run dev -w frontend"
  },
  "dependencies": {
    "next": "^14.2.0"
  }
}
